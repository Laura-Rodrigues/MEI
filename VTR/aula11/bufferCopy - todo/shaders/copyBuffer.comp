#version 430

layout(local_size_x = 1, local_size_y = 1, local_size_z = 1) in;

layout(std430, binding = 1) buffer srcBuffer {
    float srcPos[]; // sem dimensões - até acabar o buffer
}

layout(std430, binding = 2) buffer destBuffer {
    float dstPos[]; 
}

void main() {
    dstPos[gl_GlobalInvocationID.x] = srcPos[gl_GlobalInvocationID.x];
}